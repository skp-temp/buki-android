<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/white">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.example.skptemp.common.ui.Toolbar
            android:id="@+id/toolbar"
            style="@style/toolbar_size"
            app:back_button_visibility="true"
            app:layout_constraintBottom_toTopOf="@+id/view_pager"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:title_center="true" />

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginHorizontal="@dimen/horizontal_margin_large"
            android:layout_weight="1">

            <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/view_pager"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                app:layout_constraintBottom_toTopOf="@+id/next_button"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- 76 / 760 -->
            <Button
                android:id="@+id/next_button"
                style="@style/button_text_style"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_marginBottom="@dimen/button_bottom_margin"
                android:text="@string/next_button"
                android:textColor="@animator/button_selector_text_color"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintHeight_percent="0.1"
                app:layout_constraintTop_toBottomOf="@+id/view_pager"
                app:tint="@animator/button_selector_background_color" />
        </androidx.constraintlayout.widget.ConstraintLayout>

    </LinearLayout>
</layout>